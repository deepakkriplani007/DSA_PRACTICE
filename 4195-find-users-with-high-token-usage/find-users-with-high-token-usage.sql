select user_id,count(*) as prompt_count, round(avg(cast(tokens as float)),2) as avg_tokens from prompts group by user_id having count(*) >=3 and max(tokens)>round(avg(tokens),2) ORDER BY avg_tokens DESC, user_id;